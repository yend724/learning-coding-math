---
import "@/styles/common.css"
import Head from "@/components/ui/Head.astro"
import Footer from "@/components/ui/Footer.astro"
import site from "@/data/site.json"
const url = new URL(Astro.request.url);
const canonical = url.href;
---
<!DOCTYPE html>
<html lang="ja">
<head>
  <Head title={`404 Not Found | ${site.siteName}`} description={site.description} canonical={canonical} />
</head>
<body>
  <div data-wrapper="404" class="grid grid-cols-1 grid-rows-[1fr_auto]">
    <main class="flex items-center justify-center">
      <h1 class="text-4xl font-bold">404 Not Found.</h1>
    </main>
    <Footer />
  </div>
</body>
</html>
